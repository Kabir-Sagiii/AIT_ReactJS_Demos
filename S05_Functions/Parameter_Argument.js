function addNumbers(x, y) {
  var num1 = x;
  var num2 = y;
  var result = num1 + num2;
  console.log(result);
}
addNumbers(10, 20); // 10 20

addNumbers(30, 40); //30 40

addNumbers(100, 100); //100 100
